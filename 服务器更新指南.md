# 服务器更新指南

**生成时间**：2026-06-17

---

## 🔴 当前问题

| 问题 | 状态 | 说明 |
|------|------|------|
| SSH 连接 | ❌ 超时 | 端口 22 可达但连接超时 |
| HTTPS 访问 | ❌ 失败 | SSL 握手失败 |
| 后端服务 | ❌ 未运行 | 端口 8080 不通 |
| 本地代码 | ✅ 已更新 | 所有域名已替换为 susuq.top |

---

## 📋 本地已修改的文件

以下文件已在本地修改完成，需要上传到服务器：

### Server 端（5 个文件）

1. **server/internal/handler/udid.go**
   - 第 24-25 行：域名白名单改为 susuq.top
   - 第 181 行：默认跳转链接改为 susuq.top

2. **server/internal/handler/payment.go**
   - 第 198 行：支付返回跳转改为 susuq.top

3. **server/internal/handler/install.go**
   - 第 26 行：注释更新为 susuq.top

4. **server/cmd/server/main.go**
   - 第 37 行：BASE_URL 默认值改为 api.susuq.top
   - 第 329 行：CORS 白名单改为 susuq.top

5. **server/.env.example**
   - 第 3 行：BASE_URL 改为 api.susuq.top

---

## 🛠️ 手动操作步骤

### 步骤 1：获取服务器访问权限

由于 SSH 无法连接，你需要通过其他方式访问服务器：

**方式一：云服务商控制台**
- 登录阿里云/腾讯云控制台
- 找到服务器实例
- 使用 VNC 或 Workbench 登录

**方式二：检查 SSH 配置**
```bash
# 在服务器控制台执行
sudo systemctl status sshd
sudo netstat -tlnp | grep 22
sudo firewall-cmd --list-ports
```

### 步骤 2：检查 Docker 状态

```bash
# SSH 登录后执行
docker ps -a

# 如果容器停止，启动服务
cd /opt/jisign
docker compose up -d

# 查看日志
docker logs jisign-server --tail 100
```

### 步骤 3：更新服务器代码

**方式一：使用 SCP 上传文件（如果 SSH 恢复）**

在本地 PowerShell 执行：
```powershell
# 上传修改的文件
scp "C:\Users\13266\Desktop\速签\server\internal\handler\udid.go" root@39.96.76.211:/opt/jisign/server/internal/handler/
scp "C:\Users\13266\Desktop\速签\server\internal\handler\payment.go" root@39.96.76.211:/opt/jisign/server/internal/handler/
scp "C:\Users\13266\Desktop\速签\server\internal\handler\install.go" root@39.96.76.211:/opt/jisign/server/internal/handler/
scp "C:\Users\13266\Desktop\速签\server\cmd\server\main.go" root@39.96.76.211:/opt/jisign/server/cmd/server/
```

**方式二：通过控制台手动编辑**

登录服务器后，使用 vim 或 nano 缆辑文件：

```bash
# 编辑 udid.go
cd /opt/jisign/server/internal/handler
sudo vim udid.go

# 修改以下内容：
# 第 24-25 行：
# "jisign.top"     → "susuq.top"
# "www.jisign.top" → "www.susuq.top"

# 第 181 行：
# "https://jisign.top/purchase/?udid_ok=1" → "https://susuq.top/purchase/?udid_ok=1"
```

```bash
# 编辑 payment.go
sudo vim payment.go

# 修改第 198 行：
# "https://jisign.top/purchase/?paid=" → "https://susuq.top/purchase/?paid="
```

```bash
# 编辑 install.go
sudo vim install.go

# 修改第 26 行注释：
# https://api.jisign.top → https://api.susuq.top
```

```bash
# 编辑 main.go
cd /opt/jisign/server/cmd/server
sudo vim main.go

# 修改第 37 行：
# "https://api.jisign.com" → "https://api.susuq.top"

# 修改第 329 行 CORS 白名单：
# 添加 "https://susuq.top,https://www.susuq.top,https://api.susuq.top"
```

### 步骤 4：重新编译并重启服务

```bash
cd /opt/jisign

# 停止当前服务
docker compose down

# 重新编译（无缓存）
docker compose build --no-cache server

# 启动服务
docker compose up -d

# 检查状态
docker ps -a
docker logs jisign-server --tail 50
```

### 步骤 5：验证服务

```bash
# 测试 API 健康检查
curl -k https://api.susuq.top/health

# 测试网站
curl -k https://susuq.top

# 检查端口
netstat -tlnp | grep 8080
```

---

## 🔧 常见问题解决

### 问题 1：Docker 容器无法启动

```bash
# 查看详细日志
docker logs jisign-server

# 检查环境变量
cat /opt/jisign/server/.env

# 确保 .env 文件存在且配置正确
```

### 问题 2：SSL 证书错误

```bash
# 检查证书文件
ls -la /opt/jisign/server/certs/

# 检查证书有效期
openssl x509 -in /opt/jisign/server/certs/server.crt -noout -dates

# 如果证书过期，重新生成
cd /opt/jisign/server/certs
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout server.key -out server.crt \
  -subj "/C=CN/ST=Beijing/L=Beijing/O=速签/CN=susuq.top"
```

### 问题 3：端口被占用

```bash
# 检查 8080 端口
sudo lsof -i:8080

# 如果被占用，停止占用进程
sudo kill -9 <PID>
```

### 问题 4：防火墙问题

```bash
# 开放端口（CentOS/RHEL）
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --reload

# 开放端口（Ubuntu）
sudo ufw allow 8080/tcp
sudo ufw reload
```

---

## ✅ 验证清单

完成更新后，确认以下项目：

- [ ] Docker 容器正常运行
- [ ] API 健康检查返回 200
- [ ] 网站 https://susuq.top 可访问
- [ ] 端口 8080 正常监听
- [ ] 日志无错误信息

---

## 📞 技术支持

如果遇到问题，请提供以下信息：

1. `docker ps -a` 输出
2. `docker logs jisign-server --tail 100` 输出
3. `cat /opt/jisign/server/.env` 输出（隐藏敏感信息）
4. 错误截图或日志

---

**下一步**：请通过云服务商控制台登录服务器，按照上述步骤操作。
