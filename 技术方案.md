# 极签 - iOS 签名工具技术方案

**项目代号**: JiSign / 极签
**定位**: 一站式 iOS IPA 签名工具，集成证书购买 + 本地签名 + 多开分身 + 插件注入
**核心差异化**: 内置证书商城，用户无需自己找证书，一键购买即可签名

---

## 一、产品概述

### 1.1 目标用户
- iOS 用户，需要安装非 App Store 应用
- 需要应用多开（微信双开等）
- 需要插件注入（去广告、防撤回等）

### 1.2 核心价值
```
竞品（轻松签/全能签）: 证书要自己找 → 导入 → 签名
极签: 买证书 + 签名 一站搞定
```

### 1.3 商业模式
| 收入来源 | 定价 | 说明 |
|---------|------|------|
| 个人证书 | 59 元/份 | 有效期约 1 年，绑定设备 UDID |
| 企业证书 | 待定 | 不绑定设备，但易掉签 |
| 会员订阅（后期） | 待定 | 包含证书 + 增值功能 |

---

## 二、功能清单

### 2.1 P0 - MVP 必做

#### 证书商城
- [ ] 一键购买证书（公共池个人证书，59 元/份）
- [ ] 自动收集设备 UDID（App 内 + Web 描述文件）
- [ ] 调用内测侠 API 自动生成 P12 + mobileprovision
- [ ] 证书自动下载保存到 App 本地
- [ ] 证书列表管理（查看、删除、有效期）
- [ ] 证书有效性检测（调内测侠 check_cer_validate）
- [ ] 证书过期提醒（本地通知）

#### IPA 签名（本地）
- [ ] 导入 IPA 文件（从文件 App / Safari 下载 / URL）
- [ ] 选择证书 + IPA → 一键签名
- [ ] 签名进度显示
- [ ] 签名完成后直接安装
- [ ] 签名历史记录

#### 多开/分身
- [ ] 签名时修改 Bundle ID（实现多开）
- [ ] 修改 App 显示名称
- [ ] 支持同时安装多个分身

#### IPA 文件管理
- [ ] 本地 IPA 列表（导入的文件）
- [ ] IPA 信息查看（名称、Bundle ID、版本、大小）
- [ ] 删除/重命名

#### 用户系统
- [ ] 注册/登录（手机号 + 验证码）
- [ ] 微信/支付宝支付
- [ ] 订单记录
- [ ] UDID 绑定管理

### 2.2 P1 - 第二版

#### 插件注入
- [ ] 注入 dylib 动态库到 IPA
- [ ] 移除已有 dylib
- [ ] 常用插件推荐列表

#### 软件源
- [ ] 添加第三方 IPA 源（URL）
- [ ] 源内 IPA 浏览/搜索
- [ ] 一键下载到本地
- [ ] 内置推荐源

#### App 图标修改
- [ ] 自定义替换 App 图标
- [ ] 图标库

#### 内置下载器
- [ ] 浏览器访问资源站/网盘
- [ ] 下载 IPA 文件
- [ ] 断点续传

### 2.3 P2 - 第三版

#### 扫码功能
- [ ] 扫码导入证书
- [ ] 扫码导入 IPA 下载地址
- [ ] 扫码添加软件源

#### 高级签名选项
- [ ] 移除系统版本限制（修改 MinimumOSVersion）
- [ ] 修复白图标
- [ ] 自定义 Info.plist 键值对

#### 数据备份
- [ ] 应用数据导出/导入
- [ ] 掉签后重签保留数据

#### 自动续签提醒
- [ ] 证书即将过期推送通知
- [ ] 一键续购证书

---

## 三、系统架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                   用户设备                        │
│  ┌─────────────────────────────────────────────┐ │
│  │            极签 iOS App                      │ │
│  │                                              │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────────┐ │ │
│  │  │ 证书管理  │ │ 签名引擎  │ │  IPA 管理    │ │ │
│  │  │          │ │ (zsign)  │ │              │ │ │
│  │  └────┬─────┘ └──────────┘ └──────────────┘ │ │
│  │       │                                      │ │
│  │  ┌────▼─────────────────────────────────────┐│ │
│  │  │          本地存储 (证书/IPA/配置)          ││ │
│  │  └──────────────────────────────────────────┘│ │
│  └──────────────┬───────────────────────────────┘ │
└─────────────────┼─────────────────────────────────┘
                  │ HTTPS
┌─────────────────▼─────────────────────────────────┐
│               极签后端服务                          │
│  ┌──────────┐ ┌──────────┐ ┌───────────────────┐  │
│  │ 用户系统  │ │ 支付系统  │ │  证书代理服务      │  │
│  │          │ │          │ │  (对接内测侠 API)  │  │
│  └──────────┘ └──────────┘ └────────┬──────────┘  │
│                                      │             │
│  ┌──────────┐ ┌──────────┐          │             │
│  │ 订单管理  │ │ UDID收集  │          │             │
│  └──────────┘ └──────────┘          │             │
└──────────────────────────────────────┼─────────────┘
                                       │ HTTPS
                          ┌────────────▼────────────┐
                          │      内测侠 API          │
                          │  (证书供应商)             │
                          │                          │
                          │  create_udid_cer         │
                          │  check_cer_validate      │
                          │  query_cer               │
                          └──────────────────────────┘
```

### 3.2 技术选型

| 层 | 技术 | 说明 |
|----|------|------|
| **iOS App** | Swift + SwiftUI | 原生开发，签名性能最好 |
| **签名引擎** | zsign (C++) | 开源 IPA 重签名库，编译为 iOS framework |
| **后端** | Go (Gin) | 高性能，适合 API 服务 |
| **数据库** | PostgreSQL | 用户、订单、证书记录 |
| **缓存** | Redis | 验证码、Session、限流 |
| **支付** | 微信支付 + 支付宝 | 原生 SDK |
| **对象存储** | 阿里云 OSS | IPA 文件临时存储（可选） |
| **推送** | APNs | 证书过期提醒 |
| **部署** | Docker + 云服务器 | 后端部署 |

### 3.3 App 本身的分发方式

| 方式 | 实现 | 优先级 |
|------|------|--------|
| **网页直装** | 企业证书签名 App → 放在你的网站 → Safari 访问安装 | P0 主推 |
| **TrollStore** | 提供未签名 IPA 下载 → 用户用巨魔安装 | P0 备用 |
| **IPA 下载** | 网站提供 IPA → 用户用其他签名工具装 | P0 备用 |

网页直装实现：
```
https://你的域名/install

页面展示：
  - App 介绍 + 截图
  - "立即安装" 按钮
  - 点击后：itms-services://?action=download-manifest&url=https://你的域名/manifest.plist
  
manifest.plist 指向企业证书签名的 IPA 文件
```

---

## 四、核心流程详解

### 4.1 证书购买流程

```
用户点击"购买证书"
       │
       ▼
App 获取设备 UDID
  ├─ 方式1: App 内通过设备信息 API 获取（需越狱或特殊权限）
  └─ 方式2: 引导用户安装 mobileconfig 描述文件（推荐）
       │
       ▼
跳转支付（微信/支付宝 59元）
       │
       ▼
支付成功 → 后端收到回调
       │
       ▼
后端调用内测侠 API:
  POST https://www.neicexia.com/public_service/create_udid_cer
  参数: {
    udid: "用户设备UDID",
    udid_region_pool: "public",  // 公共池
    gain_cer_type: 1,            // 新签
    token: "你的内测侠API Token"
  }
       │
       ▼
内测侠返回:
  {
    p12_file_data: "base64...",           // P12 证书
    mobile_provision_file_data: "base64...", // 描述文件
    udid_batch_no: "EASC3",              // 批次号（保存用于后续查询）
    cer_appleid: "xxx@icloud.com"        // 使用的开发者账号
  }
       │
       ▼
后端保存订单记录 + 证书元数据
       │
       ▼
App 下载 P12 + mobileprovision → 保存到本地 Keychain / 沙盒
       │
       ▼
完成！用户可以用这个证书签名任意 IPA
```

### 4.2 UDID 收集流程（mobileconfig 方式）

```
1. 后端生成 mobileconfig 描述文件：
   - 包含回调 URL: https://你的域名/api/udid/callback
   - 包含 PayloadType: "Profile Service"

2. App 内用 Safari 打开: https://你的域名/udid?user_id=xxx

3. 页面引导用户安装描述文件 → 跳转"设置"

4. iOS 系统将设备 UDID POST 到回调 URL

5. 后端接收 UDID → 关联到用户 → 用于购买证书

6. 描述文件是一次性的，安装后自动移除
```

### 4.3 IPA 签名流程（App 本地）

```
用户选择 IPA + 证书
       │
       ▼
设置签名选项:
  - 新 Bundle ID（多开用，如 com.xxx.app.clone1）
  - 新 App 名称（如"微信分身"）
  - 注入 dylib（可选）
       │
       ▼
App 调用 zsign 引擎:
  zsign(
    ipa_path,
    p12_path,
    mobileprovision_path,
    p12_password: "1",  // 内测侠默认密码为1
    bundle_id: new_bundle_id,
    app_name: new_name,
    dylibs: [injected_dylib_paths]
  )
       │
       ▼
签名完成 → 生成新的 IPA 文件
       │
       ▼
调用 iOS 私有 API 安装 IPA
  或引导用户通过 itms-services 安装
       │
       ▼
安装完成 → 记录到签名历史
```

### 4.4 证书有效性检测（定时任务）

```
每天检测一次:
  POST https://www.neicexia.com/public_service/check_cer_validate
  参数: { udid_batch: "批次号", token: "xxx" }
       │
       ├─ 有效 → 不做处理
       │
       └─ 无效/即将过期 → 推送通知给用户
          "您的证书将于 X 天后过期，请及时续购"
```

---

## 五、数据库设计

### 5.1 核心表

```sql
-- 用户表
CREATE TABLE users (
    id          BIGSERIAL PRIMARY KEY,
    phone       VARCHAR(20) UNIQUE NOT NULL,
    nickname    VARCHAR(50),
    avatar_url  TEXT,
    created_at  TIMESTAMPTZ DEFAULT NOW(),
    updated_at  TIMESTAMPTZ DEFAULT NOW()
);

-- 设备表
CREATE TABLE devices (
    id          BIGSERIAL PRIMARY KEY,
    user_id     BIGINT REFERENCES users(id),
    udid        VARCHAR(100) UNIQUE NOT NULL,
    model       VARCHAR(50),          -- iPhone 15 Pro 等
    ios_version VARCHAR(20),
    created_at  TIMESTAMPTZ DEFAULT NOW()
);

-- 证书表
CREATE TABLE certificates (
    id                  BIGSERIAL PRIMARY KEY,
    user_id             BIGINT REFERENCES users(id),
    device_id           BIGINT REFERENCES devices(id),
    udid_batch_no       VARCHAR(20),      -- 内测侠批次号
    cer_appleid         VARCHAR(100),     -- 使用的开发者账号
    pool_type           VARCHAR(20),      -- public / private
    status              VARCHAR(20) DEFAULT 'active',  -- active / expired / revoked
    expire_at           TIMESTAMPTZ,
    created_at          TIMESTAMPTZ DEFAULT NOW()
);
-- 注意: P12 和 mobileprovision 文件不存数据库，只在用户设备本地保存
-- 数据库只存元数据（批次号、状态、过期时间）

-- 订单表
CREATE TABLE orders (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT REFERENCES users(id),
    order_no        VARCHAR(50) UNIQUE NOT NULL,
    product_type    VARCHAR(30),      -- personal_cert / enterprise_cert
    amount          DECIMAL(10,2),    -- 59.00
    payment_method  VARCHAR(20),      -- wechat / alipay
    payment_status  VARCHAR(20) DEFAULT 'pending', -- pending / paid / refunded
    paid_at         TIMESTAMPTZ,
    created_at      TIMESTAMPTZ DEFAULT NOW()
);

-- 签名记录表（统计用，非必须）
CREATE TABLE sign_records (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT REFERENCES users(id),
    certificate_id  BIGINT REFERENCES certificates(id),
    ipa_name        VARCHAR(100),
    bundle_id       VARCHAR(200),
    new_bundle_id   VARCHAR(200),
    new_app_name    VARCHAR(100),
    signed_at       TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 六、API 设计

### 6.1 后端 API

```
用户模块:
  POST   /api/auth/sms-code        # 发送验证码
  POST   /api/auth/login            # 手机号+验证码登录
  GET    /api/user/profile           # 获取用户信息

设备模块:
  GET    /api/udid/config            # 获取 mobileconfig 描述文件
  POST   /api/udid/callback          # UDID 回调接收（iOS 系统调用）
  GET    /api/devices                # 用户的设备列表

证书模块:
  POST   /api/cert/purchase          # 购买证书（创建订单）
  POST   /api/cert/generate          # 支付成功后生成证书（调内测侠API）
  GET    /api/cert/download/:id      # 下载证书文件（P12 + mobileprovision）
  GET    /api/cert/list              # 证书列表
  POST   /api/cert/check/:id         # 检查证书有效性
  DELETE /api/cert/:id               # 删除证书记录

支付模块:
  POST   /api/pay/wechat/create      # 创建微信支付订单
  POST   /api/pay/wechat/callback    # 微信支付回调
  POST   /api/pay/alipay/create      # 创建支付宝订单
  POST   /api/pay/alipay/callback    # 支付宝回调
  GET    /api/orders                  # 订单列表

App 分发:
  GET    /install                     # App 安装页面
  GET    /manifest.plist              # App manifest
  GET    /download/ipa                # IPA 下载
```

### 6.2 内测侠 API 对接（后端内部调用）

```go
// 内测侠 API 封装
type NeicexiaClient struct {
    BaseURL string   // https://www.neicexia.com
    Token   string   // API Token
}

// 核心方法
func (c *NeicexiaClient) CreateUDIDCert(udid string) (*CertResult, error)
func (c *NeicexiaClient) QueryCert(udid string) (*CertInfo, error)
func (c *NeicexiaClient) CheckCertValid(batchNo string) (bool, error)
func (c *NeicexiaClient) CheckP12Valid(p12Base64 string) (bool, error)
```

---

## 七、iOS App 结构

### 7.1 项目结构

```
JiSign/
├── JiSign.xcodeproj
├── JiSign/
│   ├── App/
│   │   ├── JiSignApp.swift              # App 入口
│   │   └── ContentView.swift            # 主视图
│   │
│   ├── Features/
│   │   ├── CertStore/                   # 证书商城
│   │   │   ├── CertStoreView.swift      # 购买证书页面
│   │   │   ├── CertListView.swift       # 已购证书列表
│   │   │   └── CertDetailView.swift     # 证书详情
│   │   │
│   │   ├── Signing/                     # 签名功能
│   │   │   ├── SigningView.swift         # 签名主页面
│   │   │   ├── SignOptionsView.swift     # 签名选项（改名/改ID/注入）
│   │   │   ├── SignProgressView.swift    # 签名进度
│   │   │   └── SignHistoryView.swift     # 签名历史
│   │   │
│   │   ├── IPAManager/                  # IPA 管理
│   │   │   ├── IPAListView.swift        # IPA 文件列表
│   │   │   ├── IPADetailView.swift      # IPA 信息详情
│   │   │   └── IPAImportView.swift      # 导入 IPA
│   │   │
│   │   ├── Sources/                     # 软件源（P1）
│   │   │   ├── SourceListView.swift     # 源列表
│   │   │   └── SourceBrowseView.swift   # 浏览源内 IPA
│   │   │
│   │   ├── Profile/                     # 个人中心
│   │   │   ├── ProfileView.swift        # 个人信息
│   │   │   ├── OrderListView.swift      # 订单记录
│   │   │   └── SettingsView.swift       # 设置
│   │   │
│   │   └── Browser/                     # 内置浏览器（P1）
│   │       └── BrowserView.swift
│   │
│   ├── Core/
│   │   ├── SignEngine/                  # 签名引擎
│   │   │   ├── ZSignWrapper.swift       # zsign C++ 桥接
│   │   │   ├── IPASigner.swift          # 签名逻辑封装
│   │   │   └── DylibInjector.swift      # dylib 注入
│   │   │
│   │   ├── Network/                     # 网络层
│   │   │   ├── APIClient.swift          # API 请求
│   │   │   ├── AuthManager.swift        # 登录态管理
│   │   │   └── PaymentManager.swift     # 支付管理
│   │   │
│   │   ├── Storage/                     # 本地存储
│   │   │   ├── CertStorage.swift        # 证书文件存储
│   │   │   ├── IPAStorage.swift         # IPA 文件管理
│   │   │   └── KeychainHelper.swift     # Keychain 操作
│   │   │
│   │   └── Utils/
│   │       ├── UDIDHelper.swift         # UDID 获取辅助
│   │       └── IPAParser.swift          # IPA 文件解析
│   │
│   ├── Resources/
│   │   └── Assets.xcassets
│   │
│   └── Libs/
│       └── zsign/                       # zsign C++ 源码编译为 framework
│           ├── zsign.h
│           ├── zsign.cpp
│           └── ...
│
├── JiSignServer/                        # Go 后端（独立仓库也行）
│   ├── cmd/
│   │   └── server/
│   │       └── main.go
│   ├── internal/
│   │   ├── handler/                     # API handlers
│   │   ├── service/                     # 业务逻辑
│   │   ├── repository/                  # 数据库操作
│   │   ├── neicexia/                    # 内测侠 API 客户端
│   │   └── payment/                     # 支付对接
│   ├── pkg/
│   │   ├── udid/                        # UDID mobileconfig 生成
│   │   └── middleware/
│   ├── migrations/                      # 数据库迁移
│   ├── go.mod
│   └── go.sum
│
└── Website/                             # App 安装落地页
    ├── index.html                       # 安装页面
    ├── manifest.plist                   # itms-services manifest
    └── privacy.html                     # 隐私政策
```

### 7.2 签名引擎核心（zsign 集成）

zsign 是 C++ 写的 IPA 重签名工具，需要编译为 iOS 可用的 framework。

核心功能:
- 替换 IPA 内的签名
- 修改 Bundle ID
- 修改 App 名称
- 注入/移除 dylib
- 修改 Info.plist

GitHub: https://github.com/zhlynn/zsign

编译方式:
```bash
# 交叉编译为 iOS arm64
# 需要用 Xcode 的 clang 工具链
# 具体编译脚本在开发时实现
```

---

## 八、开发计划

### Phase 1: MVP（4-6 周）

| 周 | 任务 | 产出 |
|----|------|------|
| **W1** | 项目搭建 | iOS 项目 + Go 后端 + 数据库 |
| | - Xcode 项目初始化（SwiftUI） | |
| | - Go 后端脚手架（Gin + PostgreSQL） | |
| | - 数据库 Schema 创建 | |
| | - zsign 编译为 iOS framework | |
| **W2** | 签名引擎 + IPA 管理 | App 内可以签名 IPA |
| | - zsign Swift 桥接层 | |
| | - IPA 文件导入（文件 App） | |
| | - IPA 信息解析（名称/BundleID/版本） | |
| | - 本地签名功能（证书+IPA→签名→安装） | |
| | - 多开（修改 Bundle ID + 名称） | |
| **W3** | 用户系统 + UDID | 用户注册登录 + UDID 收集 |
| | - 手机号 + 验证码登录 | |
| | - UDID mobileconfig 生成 + 回调 | |
| | - 设备管理 | |
| **W4** | 证书商城 + 支付 | 用户可以买证书 |
| | - 内测侠 API 对接（create_udid_cer） | |
| | - 微信/支付宝支付集成 | |
| | - 证书购买流程（支付→生成→下载） | |
| | - 证书管理（列表/详情/有效性检测） | |
| **W5** | UI 打磨 + App 分发 | 可用的产品 |
| | - 主界面 Tab 设计（签名/证书/IPA/我的） | |
| | - 安装落地页（网页直装） | |
| | - 企业证书签名 App 本身 | |
| | - 提供 TrollStore IPA 下载 | |
| **W6** | 测试 + 上线 | MVP 发布 |
| | - 全流程测试 | |
| | - 证书购买→签名→安装 E2E 测试 | |
| | - 部署后端 + 上线落地页 | |

### Phase 2: 增值功能（3-4 周）

| 任务 | 说明 |
|------|------|
| 插件注入 | dylib 注入/移除 |
| 软件源 | 第三方 IPA 源浏览/下载 |
| App 图标修改 | 自定义图标替换 |
| 内置下载器 | 浏览器 + IPA 下载 |
| 证书过期提醒 | 本地通知 + 推送 |

### Phase 3: 高级功能（2-3 周）

| 任务 | 说明 |
|------|------|
| 扫码导入 | 证书/IPA/软件源 |
| 高级签名选项 | 移除版本限制、修复白图标 |
| 数据备份 | 应用数据导出/导入 |

---

## 九、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| **Apple 吊销企业证书** | App 本身无法安装 | 准备多个企业证书轮换 |
| **内测侠服务不稳定** | 证书无法生成 | 本地缓存 + 备用证书供应商 |
| **内测侠涨价/停服** | 成本增加 | 后期自建开发者账号池 |
| **zsign 在 iOS 上运行限制** | 签名失败 | 充分测试，必要时用备选方案 |
| **Apple 封堵安装方式** | 新版 iOS 无法安装 | 跟进 TrollStore 等社区方案 |
| **支付资质** | 无法接入微信/支付宝 | 注册个体户/公司，申请支付商户 |

---

## 十、所需资源

| 资源 | 用途 | 预估成本 |
|------|------|---------|
| Apple 企业开发者账号 | App 本身的网页直装 | $299/年 × 2-3个 |
| 内测侠账号 + 充值 | 证书供应 | 按用户量充值 |
| 云服务器 | 后端部署 | ~200元/月 |
| 域名 + SSL | 网站 + API | ~100元/年 |
| 微信/支付宝商户 | 支付 | 需营业执照 |
| 短信服务 | 验证码 | ~0.05元/条 |
