# 支付宝商户配置指南

## 📋 当前状态检查

### 需要配置的信息

| 配置项 | 状态 | 说明 |
|--------|------|------|
| ALIPAY_APP_ID | ❌ 未配置 | 支付宝应用ID |
| ALIPAY_SELLER_ID | ❌ 未配置 | 商户PID（支付宝账号） |
| ALIPAY_PRIVATE_KEY_PATH | ❌ 未配置 | 应用私钥文件路径 |
| ALIPAY_APP_CERT_PATH | ❌ 未配置 | 应用公钥证书路径 |
| ALIPAY_PUBLIC_CERT_PATH | ❌ 未配置 | 支付宝公钥证书路径 |
| ALIPAY_ROOT_CERT_PATH | ❌ 未配置 | 支付宝根证书路径 |
| ALIPAY_NOTIFY_URL | ⚠️ 需更新 | 支付回调地址 |
| ALIPAY_RETURN_URL | ⚠️ 需更新 | 支付完成跳转地址 |

---

## 🔍 第一步：确认商户身份

### 检查方法

1. **登录支付宝开放平台**
   - 访问：https://open.alipay.com
   - 使用账号 `15895909558` 登录

2. **查看账户类型**
   - 进入「我的应用」→「账户中心」
   - 查看「账户类型」：
     - **个人账户**：需要升级为「个体工商户」或「企业账户」才能开通支付功能
     - **个体工商户**：可以开通电脑网站支付
     - **企业账户**：可以直接开通

3. **确认是否已创建应用**
   - 进入「我的应用」→「网页/移动应用」
   - 如果没有应用，需要先创建

---

## 🛠️ 第二步：开通电脑网站支付

### 如果是个人账户

**需要升级为个体工商户：**
1. 进入「账户中心」→「账户管理」→「升级为个体工商户」
2. 准备材料：
   - 身份证正反面照片
   - 营业执照照片（如果没有，可以去当地市场监管局办理）
   - 经营场所证明（可以是租赁合同或自有房产证明）

### 如果是个体工商户/企业账户

1. 进入「产品中心」→「电脑网站支付」
2. 点击「申请开通」
3. 填写应用信息：
   - 应用名称：`速签iOS签名工具`
   - 应用类型：`工具/效率`
   - 应用简介：`iOS应用签名工具，提供证书购买和签名服务`

---

## 📝 第三步：配置应用密钥

### 1. 生成密钥对

**方法一：使用支付宝开放平台工具**
1. 下载「支付宝开放平台开发助手」
   - 下载地址：https://opendocs.alipay.com/open/291/105971
2. 安装并运行
3. 选择「密钥对」→「RSA2」
4. 点击「生成密钥」
5. 保存：
   - 应用私钥（app_private_key.pem）
   - 应用公钥（app_public_key.pem）

**方法二：使用命令行**
```bash
# 生成RSA密钥对
openssl genrsa -out app_private_key.pem 2048
openssl rsa -in app_private_key.pem -pubout -out app_public_key.pem
```

### 2. 上传应用公钥

1. 进入「应用管理」→ 选择你的应用
2. 点击「查看/编辑」→「接口加签方式」
3. 选择「公钥证书模式」
4. 上传应用公钥证书：
   - 点击「上传应用公钥」
   - 选择 `app_public_key.pem`
5. 下载支付宝提供的证书：
   - 应用公钥证书（appCertPublicKey_xxx.crt）
   - 支付宝公钥证书（alipayCertPublicKey_RSA2.crt）
   - 支付宝根证书（alipayRootCert.crt）

### 3. 配置回调地址

在应用配置中设置：
- 授权回调地址：`https://api.susuq.top/api/pay/alipay/return`
- 应用网关：`https://api.susuq.top`

---

## 📁 第四步：上传证书文件

### 需要上传的文件

将以下文件上传到服务器 `/opt/jisign/server/certs/alipay/` 目录：

```
/opt/jisign/server/certs/alipay/
├── app_private_key.pem          # 应用私钥
├── appCertPublicKey_xxx.crt     # 应用公钥证书
├── alipayCertPublicKey_RSA2.crt # 支付宝公钥证书
└── alipayRootCert.crt           # 支付宝根证书
```

### 上传命令

```bash
# 在本地执行（替换实际文件路径）
scp app_private_key.pem root@39.96.76.211:/opt/jisign/server/certs/alipay/
scp appCertPublicKey_xxx.crt root@39.96.76.211:/opt/jisign/server/certs/alipay/
scp alipayCertPublicKey_RSA2.crt root@39.96.76.211:/opt/jisign/server/certs/alipay/
scp alipayRootCert.crt root@39.96.76.211:/opt/jisign/server/certs/alipay/
```

---

## ⚙️ 第五步：更新服务器配置

### 更新 .env 文件

```bash
# 在服务器上执行
cat >> /opt/jisign/server/.env << 'EOF'

# 支付宝配置
ALIPAY_APP_ID=你的应用ID
ALIPAY_SELLER_ID=15895909558
ALIPAY_PRIVATE_KEY_PATH=/app/certs/alipay/app_private_key.pem
ALIPAY_APP_CERT_PATH=/app/certs/alipay/appCertPublicKey_xxx.crt
ALIPAY_PUBLIC_CERT_PATH=/app/certs/alipay/alipayCertPublicKey_RSA2.crt
ALIPAY_ROOT_CERT_PATH=/app/certs/alipay/alipayRootCert.crt
ALIPAY_NOTIFY_URL=https://api.susuq.top/api/pay/alipay/notify
ALIPAY_RETURN_URL=https://susuq.top/purchase
ALIPAY_IS_PRODUCTION=true
EOF
```

### 更新 docker-compose.yml

确保证书目录正确挂载：
```yaml
volumes:
  - ./server/certs:/app/certs:ro
```

### 重启服务

```bash
cd /opt/jisign && docker compose restart server
```

---

## ✅ 第六步：测试支付功能

### 1. 测试环境

在「应用管理」→「沙箱环境」中：
- 使用沙箱账号测试支付流程
- 确认订单创建、支付、回调都正常

### 2. 生产环境

1. 提交应用审核
2. 审核通过后上线
3. 使用真实账号测试支付

---

## 🔐 安全注意事项

1. **私钥安全**
   - 应用私钥 `app_private_key.pem` 是最高机密
   - 不要上传到GitHub或公开仓库
   - 定期更换密钥

2. **证书管理**
   - 定期检查证书有效期
   - 支付宝证书有效期通常为1年
   - 过期前及时更新

3. **回调地址**
   - 必须使用HTTPS
   - 确保服务器能正常访问

---

## 📞 常见问题

### Q: 个人账户可以开通支付吗？
A: 需要升级为「个体工商户」或「企业账户」才能开通电脑网站支付。

### Q: 证书文件在哪里下载？
A: 在支付宝开放平台 → 应用管理 → 查看应用 → 接口加签方式 中下载。

### Q: 测试环境在哪里？
A: 在支付宝开放平台 → 沙箱环境 中获取测试账号和密钥。

### Q: 支付回调失败怎么办？
A: 检查：
1. 服务器防火墙是否开放443端口
2. SSL证书是否有效
3. 回调地址是否正确
4. 服务器日志是否有错误

---

**配置完成后，请通知我，我会帮你删除敏感文件并优化配置。**
