# 极签 — 配置说明（分享版）

> 本项目已移除原作者全部敏感信息（服务器 IP、密码、API Key、证书、私钥、域名、手机号）。
> 下面列出**你需要自己申请/生成/填写**的全部项目，按优先级排列。带 ⚠️ 的不填项目跑不起来。

---

## 一、已删除的敏感文件（你需自己补上）

| 路径 | 内容 | 怎么补 |
|------|------|--------|
| `server/.env` | 服务端密钥总配置 | 复制 `server/.env.example` → 改名 `.env`，按下方填 |
| `server/certs/alipay/` | 支付宝应用私钥 + 证书（4 个文件） | 支付宝开放平台生成（见第三节·支付宝） |
| `ios/JiSign/Resources/Certs/` | iOS 本地 HTTPS 证书 `server.key`/`server.crt`/`server.p12` | 自己签发（见第三节·iOS 本地证书） |

---

## 二、必填占位符（已在代码里用大写标记，全局搜索替换）

| 占位符 | 出现位置 | 替换成 |
|--------|----------|--------|
| `YOUR_SERVER_IP` | `ios/JiSign/Core/Network/APIClient.swift`、`ios/JiSign/Info.plist`、`ios/project.yml` | 你后端服务器的公网 IP |
| `YOUR_DOMAIN` | `web/lib/api.ts`、`ios/JiSign/Core/Network/APIClient.swift` | 你的后端域名（如 `api.你的域名.com`） |
| `CHANGE_ME_DB_PASSWORD` | `docker-compose.yml` | 自定义数据库密码（要和 `.env` 里 `DATABASE_URL` 一致） |
| `CHANGE_ME_P12_PASSWORD` | `ios/JiSign/Core/SignEngine/LocalIPAServer.swift` | 已改为固定值 `"1"`（内测侠默认密码） |

> 另：服务端 7 个文件里还硬编码了原作者域名 `jisign.top` / `jisign.com`（CORS、支付回调跳转、内置 App 源等）。
> 全局搜索 `jisign.top` 和 `jisign.com` 换成你自己的域名即可。

---

## 三、需要自己申请的第三方服务（填进 `server/.env`）

### 1. 数据库 / Redis / JWT（基础，必填 ⚠️）
- `DATABASE_URL` — 自建 PostgreSQL，密码自定（和 `docker-compose.yml` 一致）
- `REDIS_URL` — 自建 Redis，一般默认即可
- `JWT_SECRET` — 自己生成强随机串：`openssl rand -base64 32`

### 2. 内测侠 API（UDID 自动注册签名，核心功能 ⚠️）
- 变量：`NEICEXIA_TOKEN`
- 获取：内测侠官网注册账号 → 后台拿 API Token（**付费服务**，按量计费）
- 不填则 UDID 注册/签名功能不可用

### 3. 阿里云短信（验证码登录）
- 变量：`ALIYUN_ACCESS_KEY_ID` / `ALIYUN_ACCESS_KEY_SECRET` / `ALIYUN_SMS_SIGN_NAME` / `ALIYUN_SMS_TEMPLATE_CODE`
- 获取：阿里云控制台 → RAM 访问控制建 AccessKey；短信服务申请签名 + 模板
- **留空则自动走 Mock 模式**：验证码直接打印到服务端日志（自测够用，不用花钱）

### 4. 支付宝（充值付费，可选）
- 变量：`ALIPAY_APP_ID` + 4 个证书文件放回 `server/certs/alipay/`
  - `app_private_key.pem`（你的应用私钥）
  - `appCertPublicKey_xxx.crt`（应用公钥证书）
  - `alipayCertPublicKey_RSA2.crt`（支付宝公钥证书）
  - `alipayRootCert.crt`（支付宝根证书）
- 获取：支付宝开放平台 → 创建网页应用 → 申请「电脑网站支付」→ 证书模式下载 4 个证书
- 同时改 `ALIPAY_NOTIFY_URL` / `ALIPAY_RETURN_URL` 为你的域名
- 不做收费可整段留空

### 5. iOS 本地 HTTPS 证书（App 内本地安装 IPA 用）
- 文件：`ios/JiSign/Resources/Certs/` 下 `server.key` / `server.crt` / `server.p12`
- 原理：App 内起本地 HTTPS 服务，需一张你自己域名（如 `local.你的域名`）的证书
- 生成：用 Let's Encrypt 签 `local.你的域名` 证书 → 打包成 `server.p12`（设密码）→ 把密码填进 `LocalIPAServer.swift` 的 `CHANGE_ME_P12_PASSWORD`

### 6. Apple 开发者账号（编译 iOS App 必需 ⚠️）
- 编译/真机运行需要你自己的 Apple 开发者账号签名（`ios/project.yml` 里配 Team ID / Bundle ID）
- 企业分发（网页直装）需 Apple 企业开发者账号（$299/年）

---

## 四、本项目不涉及 MiMo / LLM

极签是 iOS 签名工具，**不用任何大模型 API**。无需配 MiMo、Claude、OpenAI 等 key。
（MiMo 那个是另一回事——Claude Code 的中转配置，和本项目无关。）

---

## 五、跑起来（装依赖）

```bash
# 后端
cd server && go mod download
# 或直接 Docker：先填好 .env 和 docker-compose.yml 里的密码
docker-compose up -d

# 前端
cd web && npm install && npm run dev

# iOS（需 macOS + Xcode + xcodegen）
cd ios && xcodegen generate && open JiSign.xcodeproj
```

---

## 六、检查清单（填完再跑）

- [ ] `server/.env` 已从 `.env.example` 创建并填好
- [ ] `JWT_SECRET` 已换成自己的强随机串
- [ ] `NEICEXIA_TOKEN` 已填（核心功能）
- [ ] `YOUR_SERVER_IP` / `YOUR_DOMAIN` 全部替换
- [ ] `jisign.top` / `jisign.com` 全局替换成自己域名
- [ ] `docker-compose.yml` 数据库密码已改且与 `.env` 一致
- [ ] 阿里云短信已填（或确认用 Mock 模式自测）
- [ ] 支付宝证书已放回（如需收费）
- [ ] iOS 本地证书已生成 + p12 密码已填
- [ ] Apple 开发者账号已配（编译 iOS）
