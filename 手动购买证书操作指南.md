# 手动购买证书操作指南

## 📋 概述

本文档详细说明如何手动购买和获取iOS证书，适用于在小红书等平台售卖证书的场景。

---

## 🎯 两种操作方式

### 方式一：通过速签网站操作（推荐）

**优点**：
- 界面友好，操作简单
- 自动调用API，无需手动编码
- 可以保存历史记录

**适用场景**：
- 日常售卖
- 新手用户

### 方式二：通过API工具操作

**优点**：
- 更灵活，可批量操作
- 可以自定义流程

**适用场景**：
- 批量处理订单
- 技术人员使用

---

## 📱 方式一：通过速签网站操作

### 步骤1：访问证书下载页面

1. 打开浏览器
2. 访问：`http://localhost:3000/cert-download`
   - 如果网站已部署，访问：`https://your-domain.com/cert-download`

**页面入口**：
```
首页 → 导航栏 → "下载证书" 按钮
首页 → 功能特性 → "证书下载" 卡片
首页 → 底部 → "下载证书" 按钮
```

### 步骤2：获取用户UDID

#### 方式A：用户自行获取（推荐）

指导用户通过以下方式获取UDID：

**方法1：通过速签网站获取**
1. 访问 `https://your-domain.com/udid?user_id=0`
2. 点击"安装描述文件获取 UDID"
3. 在弹窗中选择"允许"
4. 前往"设置 → 通用 → VPN与设备管理"
5. 安装描述文件
6. 系统自动获取UDID并显示

**方法2：通过iPhone设置获取**
1. 打开"设置" → "通用" → "关于本机"
2. 连续点击"序列号"多次
3. 直到显示UDID（40位字符）
4. 复制UDID

**方法3：通过iTunes获取**
1. 将iPhone连接到电脑
2. 打开iTunes（Windows）或Finder（Mac）
3. 点击设备图标
4. 点击"序列号"，切换显示UDID
5. 复制UDID

#### 方式B：手动输入UDID

如果用户已经提供了UDID，直接在页面输入框中粘贴即可。

### 步骤3：选择证书类型

在证书下载页面，选择证书类型：

| 类型 | 价格 | 适用场景 |
|------|------|---------|
| 标准版 | ¥69 | 普通用户，性价比高 |
| 加强版 | ¥99 | 需要更稳定服务 |
| 稳定版 | ¥129 | 企业用户，长期使用 |

### 步骤4：生成证书

1. 输入UDID（40位字符）
2. 选择证书类型
3. 点击"生成证书"按钮
4. 等待生成完成（通常几秒钟）

**生成成功后会显示**：
- 证书ID
- P12证书文件（base64编码）
- 描述文件（base64编码）
- 证书状态

### 步骤5：下载证书文件

#### 下载P12证书
1. 点击"下载 .p12 文件"按钮
2. 选择保存位置
3. 文件名格式：`certificate_证书ID.p12`

#### 下载描述文件
1. 点击"下载 .mobileprovision 文件"按钮
2. 选择保存位置
3. 文件名格式：`certificate_证书ID.mobileprovision`

#### 复制base64数据
1. 点击P12旁边的复制按钮，复制base64数据
2. 点击描述文件旁边的复制按钮，复制base64数据
3. 可以通过微信/QQ发送给用户

### 步骤6：发送证书给用户

#### 方式A：发送文件（推荐）
1. 将下载的P12和mobileprovision文件
2. 通过微信/QQ私信发送给用户
3. 提供导入教程

#### 方式B：发送base64数据
1. 复制P12的base64数据
2. 复制描述文件的base64数据
3. 通过微信/QQ发送给用户
4. 用户需要自行解码并保存为文件

#### 方式C：指导用户自行下载
1. 告诉用户访问证书下载页面
2. 输入UDID，点击生成
3. 用户自行下载文件

---

## 💻 方式二：通过API工具操作

### 步骤1：获取易签API Token

1. 访问易签网站：`https://sign.getp12.com`
2. 使用账号密码登录：
   - 账号：farvel
   - 密码：970302yy@
3. 登录后，在用户中心找到"API Token"或"Token管理"
4. 复制API Token

### 步骤2：使用Postman或ApiPost测试

#### 安装测试工具
- **Postman**：https://www.postman.com/downloads/
- **ApiPost**：https://www.apipost.cn/

#### 创建请求

**请求信息**：
```
请求方式：POST
请求URL：https://sign.getp12.com/api/adddevice
Content-Type：application/x-www-form-urlencoded
```

**请求参数**：
```
udid: 用户设备UDID（40位字符）
warranty: 证书类型（0躺平版 1标准版 2加强版 3稳定版）
type: 设备池类型（0公共池 1独立池 2自动选择）
token: 您的易签API Token
```

**示例请求**：
```
udid=00008110-001651DE3CBA401E
warranty=1
type=0
token=your_api_token_here
```

#### 发送请求

1. 在Postman/ApiPost中创建新请求
2. 选择POST方式
3. 输入URL：`https://sign.getp12.com/api/adddevice`
4. 选择Body → x-www-form-urlencoded
5. 添加参数：
   - udid：用户的UDID
   - warranty：1（标准版）
   - type：0（公共池）
   - token：您的API Token
6. 点击"发送"按钮

#### 解析响应

**成功响应示例**：
```json
{
  "code": 1,
  "msg": "设备已存在",
  "time": "1702621073",
  "data": {
    "id": "0OMPTG",
    "pname": "jia suo",
    "pool": 0,
    "addtime": 1700049118,
    "mobileprovision": "MII1dwYJKoZIhvcNAQcCoII1aDCCNWQYzNWdkbk***",
    "p12": "MIIL6QIBAzCCC68GCSqGSIb3DQEHAaCCC6AEggucMIILmDCCBk8***",
    "state": true
  }
}
```

**关键字段**：
- `data.p12`：P12证书的base64编码
- `data.mobileprovision`：描述文件的base64编码
- `data.id`：证书ID
- `data.state`：证书状态（true=有效）

### 步骤3：解码base64数据

#### 方法A：使用在线工具
1. 访问 base64 解码网站：https://www.base64decode.org/
2. 粘贴 `data.p12` 的值
3. 点击"Decode"
4. 下载解码后的文件，保存为 `.p12` 后缀
5. 重复步骤2-4，解码 `data.mobileprovision`，保存为 `.mobileprovision` 后缀

#### 方法B：使用Python脚本

创建 `decode_cert.py` 文件：

```python
import base64
import sys

def decode_base64_file(base64_data, output_file):
    """解码base64数据并保存为文件"""
    try:
        file_data = base64.b64decode(base64_data)
        with open(output_file, 'wb') as f:
            f.write(file_data)
        print(f"✅ 成功保存文件：{output_file}")
        return True
    except Exception as e:
        print(f"❌ 解码失败：{e}")
        return False

def main():
    if len(sys.argv) < 4:
        print("用法：python decode_cert.py <base64_data> <output_file> <type>")
        print("示例：python decode_cert.py MIIL6Q... certificate.p12 p12")
        return
    
    base64_data = sys.argv[1]
    output_file = sys.argv[2]
    cert_type = sys.argv[3]
    
    print(f"解码 {cert_type} 文件...")
    decode_base64_file(base64_data, output_file)

if __name__ == "__main__":
    main()
```

使用方法：
```bash
# 解码P12证书
python decode_cert.py "MIIL6QIBAzCCC68GCSqGSIb3DQEHAaCCC6AEggucMIILmDCCBk8..." "certificate.p12" "p12"

# 解码描述文件
python decode_cert.py "MII1dwYJKoZIhvcNAQcCoII1aDCCNWQYzNWdkbk..." "certificate.mobileprovision" "mp"
```

#### 方法C：使用PowerShell脚本

创建 `decode_cert.ps1` 文件：

```powershell
param(
    [Parameter(Mandatory=$true)]
    [string]$Base64Data,
    
    [Parameter(Mandatory=$true)]
    [string]$OutputFile,
    
    [Parameter(Mandatory=$true)]
    [string]$Type
)

try {
    Write-Host "解码 $Type 文件..."
    
    # 解码base64数据
    $fileData = [System.Convert]::FromBase64String($Base64Data)
    
    # 保存为文件
    [System.IO.File]::WriteAllBytes($OutputFile, $fileData)
    
    Write-Host "✅ 成功保存文件：$OutputFile" -ForegroundColor Green
} catch {
    Write-Host "❌ 解码失败：$($_.Exception.Message)" -ForegroundColor Red
}
```

使用方法：
```powershell
# 解码P12证书
.\decode_cert.ps1 -Base64Data "MIIL6QIBAzCCC68GCSqGSIb3DQEHAaCCC6AEggucMIILmDCCBk8..." -OutputFile "certificate.p12" -Type "p12"

# 解码描述文件
.\decode_cert.ps1 -Base64Data "MII1dwYJKoZIhvcNAQcCoII1aDCCNWQYzNWdkbk..." -OutputFile "certificate.mobileprovision" -Type "mp"
```

### 步骤4：发送证书给用户

#### 方式A：发送文件
1. 将解码后的 `.p12` 和 `.mobileprovision` 文件
2. 通过微信/QQ私信发送给用户
3. 提供导入教程

#### 方式B：发送base64数据
1. 直接复制API响应中的 `data.p12` 和 `data.mobileprovision`
2. 通过微信/QQ发送给用户
3. 用户需要自行解码

---

## 📝 完整操作流程示例

### 场景：用户在小红书私信购买证书

#### 步骤1：用户咨询
```
用户：你好，我想购买iOS签名证书，怎么操作？
```

#### 步骤2：回复用户
```
您好！购买证书很简单，请按以下步骤操作：

1️⃣ 获取设备UDID
   - 打开"设置" → "通用" → "关于本机"
   - 连续点击"序列号"多次，直到显示UDID
   - 复制40位UDID发给我

2️⃣ 支付证书费用
   - 标准版：¥69/年
   - 加强版：¥99/年
   - 稳定版：¥129/年
   - 支付方式：支付宝/微信

3️⃣ 收到UDID和付款后，我会立即为您生成证书
4️⃣ 证书生成后，我会发送P12证书和描述文件给您
5️⃣ 您通过速签App导入证书即可使用

请问您的设备UDID是多少？
```

#### 步骤3：获取UDID
```
用户：我的UDID是：00008110-001651DE3CBA401E
```

#### 步骤4：生成证书

**方式A：通过网站生成**
1. 打开浏览器，访问 `http://localhost:3000/cert-download`
2. 输入UDID：`00008110-001651DE3CBA401E`
3. 选择证书类型：标准版
4. 点击"生成证书"
5. 等待生成完成
6. 下载P12和mobileprovision文件

**方式B：通过API生成**
1. 打开Postman/ApiPost
2. 创建POST请求：`https://sign.getp12.com/api/adddevice`
3. 添加参数：
   - udid: `00008110-001651DE3CBA401E`
   - warranty: `1`
   - type: `0`
   - token: `your_api_token`
4. 点击发送
5. 复制响应中的 `data.p12` 和 `data.mobileprovision`
6. 使用Python/PowerShell脚本解码并保存为文件

#### 步骤5：发送证书给用户
```
证书已生成！请查收以下文件：

📁 P12证书文件：certificate_0OMPTG.p12
📁 描述文件：certificate_0OMPTG.mobileprovision

使用方法：
1. 下载速签App：https://your-domain.com/install
2. 打开速签App → 证书管理 → 导入证书
3. 选择上面两个文件导入
4. 导入成功后即可使用

如有问题请随时联系我！
```

#### 步骤6：用户导入证书
```
用户：收到文件了，怎么导入？

您：请按以下步骤操作：

1. 下载并安装速签App
2. 打开速签App
3. 点击底部"证书"标签
4. 点击右上角"+"按钮
5. 选择"导入证书"
6. 选择P12文件和mobileprovision文件
7. 输入证书密码（默认是"1"）
8. 点击"导入"
9. 导入成功后即可使用

如果遇到问题，请截图给我看看！
```

---

## 🔧 批量操作脚本

### Python批量生成脚本

创建 `batch_generate_cert.py` 文件：

```python
import requests
import base64
import json
import os
from datetime import datetime

# 易签API配置
YIQIAN_API_URL = "https://sign.getp12.com/api/adddevice"
YIQIAN_TOKEN = "your_api_token_here"  # 替换为您的API Token

def generate_cert(udid, cert_type=1, device_type=0):
    """
    生成证书
    
    参数：
    - udid: 设备UDID
    - cert_type: 证书类型（0躺平版 1标准版 2加强版 3稳定版）
    - device_type: 设备池类型（0公共池 1独立池 2自动选择）
    
    返回：
    - 成功返回证书信息，失败返回None
    """
    try:
        data = {
            'udid': udid,
            'warranty': str(cert_type),
            'type': str(device_type),
            'token': YIQIAN_TOKEN
        }
        
        response = requests.post(YIQIAN_API_URL, data=data, timeout=30)
        result = response.json()
        
        if result.get('code') == 1:
            cert_data = result.get('data', {})
            print(f"✅ 证书生成成功！")
            print(f"   证书ID: {cert_data.get('id')}")
            print(f"   状态: {'有效' if cert_data.get('state') else '无效'}")
            return cert_data
        else:
            print(f"❌ 证书生成失败：{result.get('msg')}")
            return None
            
    except Exception as e:
        print(f"❌ 请求失败：{e}")
        return None

def decode_and_save_cert(cert_data, output_dir):
    """
    解码并保存证书文件
    
    参数：
    - cert_data: 证书数据
    - output_dir: 输出目录
    """
    try:
        cert_id = cert_data.get('id', 'unknown')
        
        # 创建输出目录
        os.makedirs(output_dir, exist_ok=True)
        
        # 解码并保存P12证书
        p12_data = base64.b64decode(cert_data.get('p12', ''))
        p12_file = os.path.join(output_dir, f"certificate_{cert_id}.p12")
        with open(p12_file, 'wb') as f:
            f.write(p12_data)
        print(f"✅ P12证书已保存：{p12_file}")
        
        # 解码并保存描述文件
        mp_data = base64.b64decode(cert_data.get('mobileprovision', ''))
        mp_file = os.path.join(output_dir, f"certificate_{cert_id}.mobileprovision")
        with open(mp_file, 'wb') as f:
            f.write(mp_data)
        print(f"✅ 描述文件已保存：{mp_file}")
        
        return p12_file, mp_file
        
    except Exception as e:
        print(f"❌ 保存文件失败：{e}")
        return None, None

def main():
    # 示例：批量生成证书
    udid_list = [
        "00008110-001651DE3CBA401E",
        # 添加更多UDID...
    ]
    
    output_dir = f"certs_{datetime.now().strftime('%Y%m%d_%H%M%S')}"
    
    print("=" * 50)
    print("批量证书生成工具")
    print("=" * 50)
    
    for i, udid in enumerate(udid_list, 1):
        print(f"\n[{i}/{len(udid_list)}] 处理UDID: {udid}")
        
        # 生成证书
        cert_data = generate_cert(udid, cert_type=1, device_type=0)
        
        if cert_data:
            # 保存证书文件
            p12_file, mp_file = decode_and_save_cert(cert_data, output_dir)
            
            if p12_file and mp_file:
                print(f"   ✅ 证书文件已保存到：{output_dir}/")
        
        print("-" * 50)
    
    print(f"\n🎉 批量生成完成！")
    print(f"📁 证书文件保存在：{output_dir}/")

if __name__ == "__main__":
    main()
```

### PowerShell批量生成脚本

创建 `batch_generate_cert.ps1` 文件：

```powershell
param(
    [Parameter(Mandatory=$true)]
    [string]$Token,
    
    [Parameter(Mandatory=$true)]
    [string[]]$UdidList,
    
    [int]$CertType = 1,
    [int]$DeviceType = 0
)

$ApiUrl = "https://sign.getp12.com/api/adddevice"
$OutputDir = "certs_$(Get-Date -Format 'yyyyMMdd_HHmmss')"

# 创建输出目录
New-Item -ItemType Directory -Force -Path $OutputDir | Out-Null

Write-Host "=" * 50
Write-Host "批量证书生成工具"
Write-Host "=" * 50

$index = 0
foreach ($udid in $UdidList) {
    $index++
    Write-Host "`n[$index/$($UdidList.Count)] 处理UDID: $udid"
    
    try {
        # 调用API生成证书
        $data = @{
            udid = $udid
            warranty = $CertType.ToString()
            type = $DeviceType.ToString()
            token = $Token
        }
        
        $response = Invoke-RestMethod -Uri $ApiUrl -Method Post -Body $data -TimeoutSec 30
        
        if ($response.code -eq 1) {
            $certData = $response.data
            Write-Host "✅ 证书生成成功！" -ForegroundColor Green
            Write-Host "   证书ID: $($certData.id)"
            Write-Host "   状态: $(if ($certData.state) {'有效'} else {'无效'})"
            
            # 解码并保存P12证书
            $p12Data = [System.Convert]::FromBase64String($certData.p12)
            $p12File = Join-Path $OutputDir "certificate_$($certData.id).p12"
            [System.IO.File]::WriteAllBytes($p12File, $p12Data)
            Write-Host "✅ P12证书已保存：$p12File" -ForegroundColor Green
            
            # 解码并保存描述文件
            $mpData = [System.Convert]::FromBase64String($certData.mobileprovision)
            $mpFile = Join-Path $OutputDir "certificate_$($certData.id).mobileprovision"
            [System.IO.File]::WriteAllBytes($mpFile, $mpData)
            Write-Host "✅ 描述文件已保存：$mpFile" -ForegroundColor Green
        } else {
            Write-Host "❌ 证书生成失败：$($response.msg)" -ForegroundColor Red
        }
    } catch {
        Write-Host "❌ 请求失败：$($_.Exception.Message)" -ForegroundColor Red
    }
    
    Write-Host "-" * 50
}

Write-Host "`n🎉 批量生成完成！" -ForegroundColor Cyan
Write-Host "📁 证书文件保存在：$OutputDir/" -ForegroundColor Cyan
```

使用方法：
```powershell
.\batch_generate_cert.ps1 -Token "your_api_token" -UdidList @("UDID1", "UDID2", "UDID3")
```

---

## 📊 订单管理表格

建议使用Excel或在线表格管理订单：

### 订单表格模板

| 订单号 | 用户名 | 联系方式 | UDID | 证书类型 | 金额 | 支付状态 | 证书状态 | 生成时间 | 备注 |
|--------|--------|----------|------|----------|------|----------|----------|----------|------|
| 20240101001 | 小明 | 微信: xiaoming | 000081... | 标准版 | ¥69 | 已支付 | 已生成 | 2024-01-01 | |
| 20240101002 | 小红 | QQ: 123456 | 000081... | 加强版 | ¥99 | 已支付 | 已生成 | 2024-01-01 | |

### 状态说明

- **待支付**：用户已提供UDID，等待付款
- **已支付**：用户已付款，等待生成证书
- **已生成**：证书已生成，等待发送给用户
- **已交付**：证书已发送给用户
- **已完成**：用户确认收到并导入成功
- **已退款**：证书已退款

---

## ⚠️ 常见问题处理

### 问题1：证书生成失败

**可能原因**：
- UDID格式错误
- API Token无效
- 易签账户余额不足
- 网络连接问题

**解决方法**：
1. 检查UDID是否为40位字符
2. 检查API Token是否正确
3. 登录易签网站查看余额
4. 检查网络连接

### 问题2：证书文件无法导入

**可能原因**：
- P12和描述文件不匹配
- 证书密码错误
- 文件损坏

**解决方法**：
1. 确保P12和描述文件是同一UDID生成的
2. 尝试使用默认密码"1"
3. 重新生成证书

### 问题3：证书掉落

**可能原因**：
- Apple吊销了开发者证书
- 证书过期

**解决方法**：
1. 告知用户这是Apple官方行为
2. 提供免费重新生成服务
3. 提醒用户续费

---

## 📞 技术支持

如果遇到问题，请按以下顺序排查：

1. **检查网络连接**
2. **检查API Token是否有效**
3. **检查易签账户余额**
4. **检查UDID格式是否正确**
5. **查看易签网站状态**

如需进一步帮助，请联系：
- 易签客服：https://sign.getp12.com
- 速签客服：https://your-domain.com
